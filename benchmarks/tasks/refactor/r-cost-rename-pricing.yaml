id: r-cost-rename-pricing
category: refactor
set: dev
repo: https://github.com/ittakestwo123/Harnesslab.git
commit: 075550a6f3ec01f10eb846d8e61181e4495f082b
prompt: |
  Rename the private field `pricing` of the Calculator type in
  internal/cost/cost.go to `priceTable` and update every reference to it in
  that package (the constructor and the Calculate method). Do not change any
  exported API, behavior or test files. Use the file tools to edit cost.go.
  All tests in ./internal/cost/... must still pass.
verification:
  strategy: final
  commands:
    - findstr "priceTable" internal\cost\cost.go
    - go test ./internal/cost/...
